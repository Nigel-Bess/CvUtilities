<UserControl x:Class="CvBuilder.Ui.Deploy.BuildAndDeployControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CvBuilder.Ui.DeployDispense"
             xmlns:outer="clr-namespace:CvBuilder.Ui"
              xmlns:t="clr-namespace:CvBuilder.Ui.Terminal"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=local:BuildAndDeployViewModel}">
    <UserControl.Resources>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="Label">
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Width" Value="125"/>
            <Setter Property="Height" Value="25"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <StackPanel Orientation="Vertical">
                <StackPanel Orientation="Horizontal">
                    <Label Content="Branch Name:"/>
                    <TextBox Text="{Binding BuildBranchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Label Content="Target Facility:"/>
                    <ComboBox SelectedItem="{Binding SelectedFacility}" ItemsSource="{Binding FacilityOptions}" Width="150" HorizontalContentAlignment="Center" VerticalContentAlignment="Center"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Label Content="Builld Box:"/>
                    <ComboBox SelectedItem="{Binding SelectedBuildBox}" ItemsSource="{Binding BuildBoxOptions}" Width="150" HorizontalContentAlignment="Center" VerticalContentAlignment="Center"/>
                </StackPanel>
                <UniformGrid Rows="1">
                    <Button Content="Deploy Without Building" Command="{Binding DeployWithoutBuildingCommand}"/>
                    <Button Content="Start Build" Command="{Binding StartBuildCommand}"/>
                </UniformGrid>
            </StackPanel>

            <GroupBox Header="Completed Builds" Grid.Row="1">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding DeployableBuilds}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Background="LightBlue" Margin="5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                
                                    <StackPanel Orientation="Horizontal" >
                                        <Label Content="{Binding BuildName}"/>
                                        <Button Content="Deploy" Command="{Binding DeployCommand}" Margin="5,0"/>
                                    
                                    </StackPanel>
                                    <Button Content="Remove" Command="{Binding RemoveCommand}" Grid.Column ="1"/>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </GroupBox>
            <outer:ScriptRunReportControl Grid.Row="2" DataContext="{Binding ScriptRunner.ReportViewModel}" Height="25"/>


        </Grid>
        <t:TerminalView Grid.Column="1" DataContext="{Binding ScriptRunner.Terminal}"/>
       
    </Grid>
</UserControl>
